
if ( !("IRL_adm1.shp" %in% list.files()) ){cat("Ireland Counties shapefile not present in working directory.\n")}
if ( !("USA_adm1.shp" %in% list.files()) ){cat("USA States shapefile not present in working directory.\n")}
if ( !("USA_adm2.shp" %in% list.files()) ){cat("USA Counties shapefile not present in working directory.\n")}
